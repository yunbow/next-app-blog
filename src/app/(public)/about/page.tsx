import { AboutContent } from "@/components/about/AboutContent";

export const metadata = {
  title: "About | Blog",
  description: "About the creator of Blog",
};

export default function AboutPage() {
  return <AboutContent />;
}
